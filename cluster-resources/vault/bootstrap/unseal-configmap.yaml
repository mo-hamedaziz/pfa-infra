apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-unseal
  namespace: vault
data:
  unseal.sh: |
    #!/bin/sh
    echo "Unsealing Vault..."
    until vault status; do
      sleep 2
    done
    vault operator unseal $(cat /vault/config/unseal-key)